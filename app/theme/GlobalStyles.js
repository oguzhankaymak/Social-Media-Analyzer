import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  center: {
    alignItems: 'center',
    justifyContent: 'center',
  },

  fCenter: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
